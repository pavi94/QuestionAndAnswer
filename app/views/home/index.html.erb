<div class="well">
	<h4>Creating a healthier tomorrow, today</h4>
	<p>We're committed to improving the health and care of individuals from their communities - from day one.Health and care connected We're connecting your health information across facilities, so your information goes with you.
	</p>
</div>
<div class="container">
  <% @questions.each do |q| %>
    <div class="media" data-target="<%= question_path(q) %>">
      <div class="media-left">
        <a href="#">
          <img class="media-object" src="<%= q.gravatar %>">
        </a>
      </div>
      <div class="media-body">
        <h4 class="media-heading"><%= q.email %> asked:</h4>
        <div class="time_ago"><%= time_ago_in_words q.created_at %> ago</div>
        <div><%= truncate q.body, length: 20 %></div>
        <div><a href="<%= question_path(q) %>" class="btn btn-success btn-xs">View Answers</a></div>
      </div>
    </div>
  <% end %>

  <% if @questions.empty? %>
    <div class="alert alert-info">Nothing here yet, be the first to ask?</div>
  <% end %>
</div>